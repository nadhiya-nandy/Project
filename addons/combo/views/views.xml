<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="combo.list">
      <field name="name">combo list</field>
      <field name="model">combo.combo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="combo.action_window">
      <field name="name">combo window</field>
      <field name="res_model">combo.combo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="combo.action_server">
      <field name="name">combo server</field>
      <field name="model_id" ref="model_combo_combo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="combo" id="combo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="combo.menu_1" parent="combo.menu_root"/>
    <menuitem name="Menu 2" id="combo.menu_2" parent="combo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="combo.menu_1_list" parent="combo.menu_1"
              action="combo.action_window"/>
    <menuitem name="Server to list" id="combo" parent="combo.menu_2"
              action="combo.action_server"/>
    -->
  </data>
</odoo>